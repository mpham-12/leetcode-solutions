/**
 * @param {number} n - a positive integer
 * @return {number}
 */
//INCOMPLETE
 var hammingWeight = function(n) {
    const obj={};
    const arr= Array.from(String(n), Number);
    console.log(arr)
    // for (bit of arr){
    //   obj[bit]=(obj[bit]||0)+1;
    // }
// console.log(obj)
};

console.log(hammingWeight(000001))
// console.log(hammingWeight(11111111111111111111111111111101))